{
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "prepare": "husky"
  },
  "dependencies": {
    "vue-chartjs": "^5.3.3"
  }
}
